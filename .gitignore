# Ignore files generated by running 'bootstrap' script
**/Makefile.in
config.h.in
aclocal.m4
autom4te.cache/*
configure
autotools/**
!autotools/ac_c_compile_flags.m4
!autotools/ac_cpu_optimisations.m4

# Ignore files generated either by running 'configure' script or through the 'make' build process
# (assuming that the build directory (i.e. the current working director from which the 'configure' script was called)
# is the local directory named 'build')
build/*
