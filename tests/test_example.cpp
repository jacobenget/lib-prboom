#include "gtest/gtest.h"

/*
    This file is just an example set of unit tests, which accomplish nothing
   other than demonstrating the most basic usage of googletest.
*/

TEST(CategoryTest, SpecificTest) { ASSERT_EQ(0, 0); }
